{
  "name": "mission-crafter",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
